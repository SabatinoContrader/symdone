export class Sintomo {
    idsintomo: string;
    nomeSintomo: string;

    constructor(idsintomo: string, nomeSintomo: string) {
        this.idsintomo = idsintomo;
        this.nomeSintomo = nomeSintomo;
    }
}